# Azure Terraform Builder (Vite + React)

Includes Load Balancer and Application Gateway (HTTP-only) components, plus the full UI shell.

## Quick start
```bash
cd azure-terraform-builder
npm install
npm run dev
# open http://localhost:5173
```
